load("@rules_rust//rust:defs.bzl", "rust_binary")
load("//bazel:kernel_crates.bzl", "crate_label", "kernel_crate_label")

package(default_visibility = ["//visibility:public"])

rust_binary(
    name = "test-kernel",
    srcs = ["src/main.rs"],
    crate_root = "src/main.rs",
    edition = "2024",
    linker_script = "//kernel/boot:linker-x86_64.ld",
    platform = "//platforms:x86_64-none",
    deps = [
        "//kernel/boot:lib",
        kernel_crate_label("device"),
        kernel_crate_label("ext2"),
        kernel_crate_label("vfs"),
        crate_label("spin"),
        crate_label("tracing"),
        crate_label("x86_64"),
    ],
)
